apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleet.fullname" . }}-test-connection"
  labels:
    {{- include "fleet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
